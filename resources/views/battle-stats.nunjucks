{% extends 'master.nunjucks' %}

{% block title %}Battle Stats for {{ username }} | RangerSteve.io{% endblock %}

{% block content %}
<span id="beta-label">Beta</span>

<section class="main-section" id="battle-stats-container" v-cloak>
  <div class="col-xs-12">
    <div class="text-center">
      <span class="main-header">
        Battle Stats
      </span>
    </div>

    <div class="container">
      <div class="col-md-10 col-md-offset-1">
        <div class="well tcw">
          <div class="row mb3">
            <div class="col-sm-4">
              <a class="btn btn-primary btn-lg btn-block" href="/">Back to Menu</a>
            </div>
            <div class="col-sm-4">
              <a class="btn btn-primary btn-lg btn-block" href="/leaderboards">Leaderboards</a>
            </div>
            <div class="col-sm-4">
              <a class="btn btn-success btn-lg btn-block" href="/game">Play Now</a>
            </div>
          </div>

          <div class="row">
            <div class="col-sm-12">
              <h1 class="fwbold text-center mt3 mb6">{{ username }}</h1>

              <fieldset class="text-center">
                <legend>Overview</legend>

                <div class="row">
                  <div class="col-sm-12">
                    <div class="row text-center">
                      <div class="col-xs-3">
                        <div className="player-achievement">
                          <h4>KDR</h4>
                          <h2 class="fwbold">${ playerScore.kdr }</h2>
                        </div>
                      </div>
                      <div class="col-xs-3">
                        <div className="player-achievement">
                          <h4>Score / Min</h4>
                          <h2 class="fwbold">${ playerScore.scorePerMinute }</h2>
                        </div>
                      </div>
                      <div class="col-xs-3">
                        <div className="player-achievement">
                          <h4>Kills / Min</h4>
                          <h2 class="fwbold">${ playerScore.killsPerMinute }</h2>
                        </div>
                      </div>
                      <div class="col-xs-3">
                        <div className="player-achievement">
                          <h4>Win Percentage</h4>
                          <h2 class="fwbold">${ playerScore.winPercentage }</h2>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </fieldset>

              <fieldset class="text-center mb0">
                <legend>Statistics</legend>

                <div class="row">
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Accuracy</h4>
                      <h4 class="fwbold">${ playerScore.accuracy }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Kills</h4>
                      <h4 class="fwbold">${ playerScore.kills }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Deaths</h4>
                      <h4 class="fwbold">${ playerScore.deaths }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Headshots</h4>
                      <h4 class="fwbold">${ playerScore.headshots }</h4>
                    </div>
                  </div>
                </div>

                <div class="row">
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Shots Fired</h4>
                      <h4 class="fwbold">${ playerScore.bulletsFired }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Shots Hit</h4>
                      <h4 class="fwbold">${ playerScore.bulletsHit }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Times Hit</h4>
                      <h4 class="fwbold">${ playerScore.timesHit }</h4>
                    </div>
                  </div>
                  <div class="col-xs-3">
                    <div className="player-achievement">
                      <h4>Best Killing Spree</h4>
                      <h4 class="fwbold">${ playerScore.bestKillingSpree }</h4>
                    </div>
                  </div>
                </div>
              </fieldset>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{% endblock %}

{% block scripts %}
<script>
  new Vue({
    el: '#battle-stats-container',
    delimiters: ['${', '}'],
    data: {
      leaderboards: {},
      playerScore: {},
      uid: {},
    },
    beforeMount: function() {
      this.fetchUsernameLookup()
    },
    methods: {
      fetchUsernameLookup: function () {
        var self = this
        var ref = firebase.database()
          .ref('username_lookup/{{ username }}')
          .once('value', function(snapshot) {
            self.uid = snapshot.val()
            self.renderBattleStats()
          })
      },
      renderBattleStats: function () {
        var self = this
        var ref = firebase.database()
          .ref('leaderboards/' + self.uid)
          .once('value', function(snapshot) {
            self.leaderboards = snapshot.val()

            self.playerScore.kdr = _.get(self, 'leaderboards.deaths', 0) > 0
              ? self.leaderboards.kills / self.leaderboards.deaths
              : _.get(self, 'leaderboards.kills', 0)

            self.playerScore.accuracy = _.get(self, 'leaderboards.bulletsFired', 0) > 0
              ? (self.leaderboards.bulletsHit / self.leaderboards.bulletsFired).toFixed(1)
              : _.get(self, 'leaderboards.bulletsHit', 0)

            var minutesInRound = _.get(self, 'leaderboards.secondsInRound', 0) / 60
            self.playerScore.killsPerMinute = minutesInRound > 0
              ? (_.get(self, 'leaderboards.kills', 0) / minutesInRound).toFixed(1)
              : 0
            self.playerScore.scorePerMinute = minutesInRound > 0
              ? (_.get(self, 'leaderboards.score', 0) / minutesInRound).toFixed(1)
              : 0

            var roundsWon = _.get(self, 'leaderboards.roundsWon', 0)
            var roundsLost = _.get(self, 'leaderboards.roundsLost', 0)
            var totalRounds = roundsWon + roundsLost
            self.playerScore.winPercentage = totalRounds > 0
              ? (roundsWon / (roundsWon + roundsLost) * 100).toFixed(0) + '%'
              : '0%'

            self.playerScore.kills = _.get(self, 'leaderboards.kills', 0)
            self.playerScore.deaths = _.get(self, 'leaderboards.deaths', 0)
            self.playerScore.bestKillingSpree = _.get(self, 'leaderboards.bestKillingSpree', 0)
            self.playerScore.damageInflicted = _.get(self, 'leaderboards.damageInflicted', 0)
            self.playerScore.headshots = _.get(self, 'leaderboards.headshots', 0)
            self.playerScore.bulletsHit = _.get(self, 'leaderboards.bulletsHit', 0)
            self.playerScore.bulletsFired = _.get(self, 'leaderboards.bulletsFired', 0)
            self.playerScore.timesHit = _.get(self, 'leaderboards.timesHit', 0)
            self.$forceUpdate()
          })
      }
    }
  })
</script>
{% endblock %}
